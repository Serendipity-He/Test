/* 漫谈:-),并非梳理,但是梳理听起来`高级`*/

/*此文档不定时更新*/



## **序言**

我们把整个系统分为用户端和管理端。

接下来提到的`注册`实际上都指的是管理端手动添加单个账号

这篇文档是基于技术栈使用`Springboot+vue`的前提下的 (っ °Д °;)っ // 好消息！学校有自己的服务器ψ(｀∇´)ψ

## **总体**

### **1.权限管理**

权限管理将用户分为`User` `Admin` `Verify`

`User`: 用户端的用户，一般来说是上传资料的学生

`Admin`: 管理端的管理员，也有审核的权限 

审核 ，人工审核必不可少，可以由`Admin`分配账号,以在综测计算时期或者平时进行审核:

1. `Verify_A`:  A对应每班审核的班长，进行审核和数据汇总和检查

2. `Verify_B`: 普通审核人员，只有审核权限

### **2.账号分配**

`User`账号由管理端分配，其他信息由学生或者管理端填//存疑

**一个需要解决的问题在于安全性的问题，即如何防止其他人登录账号**

应当有添加账号功能 , 并需要进行唯一性检测

### **3.安全性**

1.密码，以及用户信息需要进行加密，以免信息泄露

2.在编码时应当注意要预防常见`xss`攻击或`sql`注入

3.在涉及删除等操作时应当提供确认环节

### **4.注册和登录优化**

系统在系内试运行时，人数在千人级别，之后如果在全校使用人数在万人级别

添加账号时的唯一性确定(确定没有学号重复)和登录 需要进行优化

下面的可选由于太重大概不会用到，只是为了以防万一

#### 注册

1.唯一性确定需要异步校验 

2.数据库读写分离（可选）

3.使用到分布式锁和布隆过滤器以确保唯一性（可选）

#### 登录

1.登录使用JWT校验

2.使用Redis缓存用户信息

### 5.资源存储

1.不能使用本地存储！使用对象存储。

2.在项目初期可以暂时使用本地存储测试

**3.开发期间使用云服务器，以便测试和协作**

4.定期备份策略

5.图片可能需要CDN加速

### **6.数据库表**

复杂的问题，可能最好提前设计出来，但是不太可能 // 想和做是两件事啊ε(┬┬﹏┬┬)3

### **7.页面设计**

尽管只是一个偏向管理的系统，但是仍然需要进行页面原型设计

目前正在进行APP的页面原型设计

### **8.端的问题**

移动端APP要做出来,对于用户来说APP会方便很多，相较于小程序，APP性能更好，更契合平时频繁使用的场景。

小程序可以后期支持.

同时网页的移动端适配也要做

APP只支持用户端(为了减轻工作量以及个人感觉大家拿手机审核很奇怪以及如果管理端也做APP的话那么需要两个APP或者一个APP要做进去两套逻辑，复杂！)

### **9.审核问题**

可以确定的是：

1.要求一份资料，**同时只能有一个人来审核**，因此考虑后端分发数据的方式，前端采用点击按钮随机一份资料来审核,每份资料数据加上一个状态字段，用于标识是否分发，同时需要设计超时时间，超时自动收回（可以长一点，比如说1h）

问题在于:

1.是以人为单位还是以每一项为单位

2.个人同意后者，但是采取分Mn的方式，比如说我选择M12我收到的资料全为M12的资料

## **用户端**

### **1.注册(手动添加账号)和登录**

`User`由学号进行唯一标识 ，其他信息例如手机号，身份证号 // 这些暂未确定

## **管理端**

管理端由于分为`Admin`和`Verify`两种权限的用户，需要进行严格的权限管理

## **编码规范**

1. 在编码时应当注意要预防常见`xss`攻击或`sql`注入
2. 后端应当使用全局异常管理机制，并在异常时打印出完整的报错堆栈信息，以便debug
3. 后端应当使用AOP记录日志和性能监控
4. 前端应当做到样式风格统一，布局**严格**按照页面原型
5. 代码规范！！！代码规范！！！代码规范！！！
6. 后端应当使用`Knife4j`来形成接口文档，同时后端在编码时要理解本项目的后端服务要为用户端(web)  , 管理端(web) , 用户端APP , 甚至是之后的小程序服务。
7. **鼓励使用AI**，但是要对AI写的代码做好审查和核实。如果你使用`Vscode`开发可以使用`Github Copilot`,IDEA也可以使用？但是笔者没有用过。
7. `Git`提交时至少要对本次做出的更改做出概括，控制每次git提交时的代码修改量，尽量多而频繁的提交代码而不是攒着很长时间才提交
7. 分支管理，如果在一个大模块，比如说两个人甚至更多人开发后端，要防止分支冲突,简单的说就是两个人不能同时修改同一个文件的代码，例如A修改了 a.java b.java c.java 而B修改了b.java d.java 那么不论A或B谁先push 后push的人都会因为和远程分支有冲突而无法push，最终只有一个人的工作成果可以保留
7. 接上面，尽量不要在主分支上更改，每一个大模块中都分配一个人来维护主分支，其他每个人都有自己的开发分支，其他人开发完成后提交PR再由维护主分支的人来审核和合并分支，至于这个维护者，除了维护主分支外，也应当有一个自己的开发分支，在她/他提交的时候，可以直接发起MR。
7. 文件上传: 文件上传要保证真删除以及备份问题，防止数据爆炸或者数据丢失的问题
